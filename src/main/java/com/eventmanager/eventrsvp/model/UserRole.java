package com.eventmanager.eventrsvp.model;

/**
 * Enum representing the roles a user can have in the system.
 * ADMIN users can manage all resources, while USER role is for regular event participants.
 */
public enum UserRole {
    ADMIN,
    USER
}
